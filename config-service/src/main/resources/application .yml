
---
server:
  port: "8891"
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/stackroute/capgemini-w1-godeliver/tree/v1.0.0/config-service/src/main/resources
  application:
    name: configserver
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka
    register-with-eureka: "true"
    fetch-registry: "true"
...
