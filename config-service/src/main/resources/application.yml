server:
  port: "8891"
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/tejakathari007/go-deliver
          force-pull: 'true'

eureka:
  client:
    register-with-eureka: "true"
    fetch-registry: "true"
    service-url:
      defaultZone: http://172.23.239.100:8761/eureka

management:
  endpoints:
    web:
      exposure:
        include: '*'