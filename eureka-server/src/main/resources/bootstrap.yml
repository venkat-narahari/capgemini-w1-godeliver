server:
  port: "9070"
eureka:
  client:
    register-with-eureka: "false"
    fetch-registry: "false"
spring:
  application:
    name: EurekaServer
management:
  endpoints:
    web:
      exposure:
        include: '*'
